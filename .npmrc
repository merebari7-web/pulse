# @react-three/fiber 9 declares a peer range that resolves against React 19.3;
# this project pins React 19.2.0 (the version the verified install used), so npm's
# strict peer resolution has to be relaxed for every install, not just mine.
legacy-peer-deps=true
